// const test = require('tape');
// var getFiles = require('../src/main-process/get-files');

// test('getFiles', function(t) {
//     var TEST_DIR = '../Music/Mouth Sounds/';

//     getFiles(TEST_DIR)
//         .then(files => {
//             t.ok(files.length > 0, 'finds files');

//             files.forEach(value => {
//                 t.ok(value.title);
//                 t.ok(value.path);
//             });

//             t.end();
//         })
//         .catch(err => t.error(err));
// });
